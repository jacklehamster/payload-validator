{
  "name": "example",
  "module": "src/index.tsx",
  "type": "module",
  "scripts": {
    "start": "bun run build && bun run index.ts --watch",
    "build": "bun i && bun build --target=node ./src/index.tsx --outfile=dist/index.js",
    "build-icon": "icon-gen -i ../icon.png --ico -o ."
  },
  "devDependencies": {
    "baojs": "^0.2.1",
    "bun-types": "^1.1.13",
    "icon-gen": "^4.0.0",
    "serve-static-bun": "^0.5.3"
  },
  "peerDependencies": {
    "typescript": "^5.0.0",
    "@dobuki/payload-validator": "link:@dobuki/payload-validator"
  }
}
